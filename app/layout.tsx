export const metadata = {
  title: 'Atlas',
  description: 'Player-driven stock economy'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
